import React from 'react';

import styles from '@/components/Article/ArticleHelper.module.css'
import { Title, Subtitle1, Subtitle2, Text, Img, Multi, Quote, Gap, Empty } from '@/components/Article/ArticleHelper';

const Article0 = (props:{
        mode: Mode,
        data: ArticleMetadata
    }
) => {
    return (
        <div className={styles.container}>
            
        </div>
    );
}

export default Article0;